---
- name: Run httpd container
  community.docker.docker_container:
    name: httpd
    image: neymarjr92/tp-devops-httpd:latest
    pull: true
    state: started
    restart_policy: always
    networks:
      - name: app-network
    ports:
      - "80:80"
